# Dependencies for the llm_agent_governor examples.
#
# Tested against anthropic 0.101.0 on Python 3.14 (2026-05-12).
# The example uses: anthropic.AsyncAnthropic, anthropic.APIError,
# messages.create with tool_use blocks — all stable since anthropic 0.40.
#
# Install in a fresh venv:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt -e ../..

rein-ai
anthropic>=0.40
